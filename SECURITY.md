# Security Policy

## این پروژه با چه داده‌هایی کار می‌کند؟

pipeline کانفیگ‌هایی را پردازش می‌کند که **عمومی** منتشر شده‌اند. با این حال خروجی‌ها
حاوی credential (UUID / رمز) هستند، بنابراین:

- **هیچ credential ای در لاگ‌ها چاپ نمی‌شود** — فقط host/port/پروتکل و نام.
- **داشبورد عمومی هیچ UUID یا رمزی ذخیره نمی‌کند** (`dashboard/data.json` فقط نام،
  پروتکل، کشور، امتیاز و وضعیت دارد). یک تست این را به‌صورت خودکار بررسی می‌کند.
- **Secrets (توکن تلگرام/دیسکورد) فقط از environment خوانده می‌شوند** و هرگز در
  مخزن commit نمی‌شوند. اگر تنظیم نشوند، اطلاع‌رسانی بی‌صدا غیرفعال می‌شود.

## نکتهٔ مهم دربارهٔ منابع عمومی

کانفیگ‌های عمومی ممکن است توسط شخص ثالث دیده یا لاگ شوند. اگر به کانفیگی نیاز دارید
که محرمانه بماند، آن را در `links.txt` قرار ندهید و از ساب خصوصی خودتان استفاده کنید.
این پروژه هیچ کانفیگی تولید یا میزبانی نمی‌کند؛ فقط آنچه عمومی منتشر شده را جمع‌آوری
و مرتب می‌کند.

## ممیزی خودکار

`popvpn/security.py` کانفیگ‌های پرخطر را علامت‌گذاری می‌کند:

| علامت | معنا |
| --- | --- |
| `allow-insecure` | اعتبارسنجی گواهی TLS خاموش است |
| `legacy-vmess` | VMess با `v: 1` (غیر AEAD، منسوخ) |
| `private-host` | هاست loopback/private/link-local است |
| `weak-credential` | رمز placeholder یا الگوی ضعیف |
| `unknown-ss-method` | متد Shadowsocks ناشناخته |

در تنظیمات production فعلی، موارد پرخطر (از جمله `weak-credential`) به‌صورت پیش‌فرض با `security.drop_*` حذف می‌شوند. این گزینه‌ها فقط برای بررسی کنترل‌شدهٔ یک feed قابل خاموش‌کردن هستند.

## گزارش آسیب‌پذیری

اگر آسیب‌پذیری امنیتی در خود کد پیدا کردید، لطفاً Issue عمومی باز **نکنید**.
از طریق «Report a vulnerability» در تب Security مخزن در GitHub اطلاع دهید تا
ابتدا اصلاح شود.

## پشتیبانی

| نسخه | وضعیت |
| --- | --- |
| 2.x | ✅ پشتیبانی می‌شود |
| 1.x | ⚠️ فقط اصلاحات امنیتی |
